# 자산 인사이트 — 작업 지침

국장·미장·매크로·코인 데이터를 모아 폰에서 보는 앱. 수집은 GitHub
Actions가, 인사이트 글은 Claude가 쓴다.

배포: https://enair840926-star.github.io/insight/

---

## 이 프로젝트의 원칙

### 추측하지 말고 재라

이 저장소에서 가장 많이 틀렸던 순간은 전부 "될 것 같다"로 시작했다.
러너에서 네이버가 막힐 거라 걱정했지만 열렸고, 피드가 읽힐 거라
생각했지만 프록시가 막았다. 재는 도구가 이미 있다.

```
python tools/probe.py            # 데이터 소스 접속 검증 (키 상태까지)
python tools/pending.py -v       # 인사이트를 다시 써야 하는 자산군
python tools/lint_insight.py     # 인사이트에 전문용어가 남았는지
```

`probe.py`는 러너에서도 돈다 — Actions의 `데이터 소스 접속 검증`을
돌리면 결과가 `notes/probe-github.md`로 커밋된다. **200 응답을 성공으로
세지 마라.** DART는 오류도 `200 + {"status":"012"}`로 주고, 그걸 성공으로
집계해 90바이트 오류 응답이 전부 ✓로 찍힌 적이 있다.

새 소스를 붙일 때는 `tools/probe.py`에 먼저 넣고 러너에서 재 본다.
PC에서 되는 것이 러너에서 된다는 보장이 없고 그 반대도 마찬가지다.

### 없는 것과 못 받은 것을 구분하라

이 구분이 무너지면 "내부자 매매가 없었다"처럼 사실이 아닌 문장이
만들어진다. 수집이 실패하면 조용히 빠지게 두지 말고 그 사실을
프롬프트·화면·피드에 남긴다.

- `collect_kr.py` — DART/ECOS 실패 시 "받지 못한 것"이라고 명시
- `core/feed.py` — `missing` 배열에 이유까지
- `dashboard.py` — 패널마다 수집 시각, 18시간 넘으면 표시

### 판정과 숫자를 섞지 마라

매크로 데스크(`enair840926-star/-`)와 숫자만 주고받는다. `stance`나
호재·악재 판정을 넘기면 그쪽 스코어보드가 자기 룰셋이 아니라 이쪽
판단을 채점하게 된다. 두 시스템의 판단이 갈리는 것 자체가 정보다.

같은 이유로 뉴스도 제목·날짜·출처만 넘기고 `label`·`score`는 뺀다.

### 코인 파생 지표는 거래소가 바뀐다

바이낸스가 미국 IP를 막는다(451, 약관상 지역 제한). 그래서 PC에서
받으면 바이낸스, 클라우드에서 받으면 OKX다. 우회하지 않고 대체한다.

**펀딩비·롱숏비의 절대 수준을 어제와 오늘 비교하면 안 된다.** 거래소가
바뀌었을 뿐인데 값이 달라진다. 실측에서 큰손·소매 롱 비율 격차의
부호가 두 거래소에서 반대로 나온 적도 있다 — 상위계좌 정의가 다르기
때문이다. 방향과 순위는 유효하지만 크기는 같은 스냅샷 안에서만 본다.

출처는 `derivatives_source`에 남고 프롬프트·화면에도 표시된다.

### 픽은 재서 고르고, 글은 서술만 한다

고르는 일을 글 쓰는 쪽에 맡기면 어제 픽과 오늘 픽이 같은 잣대로 뽑혔는지
알 수 없다. `core/pick.py`가 계산해 프롬프트에 찍고, 글은 순위와 방향을
바꾸지 않는다. 채점기는 자산군마다 두지 않고 하나다 — 없는 필드는
건너뛰므로 국장의 외국인 수급과 코인의 펀딩비가 같은 함수를 통과한다.

**이 데이터는 설명치지 예측치가 아니다.** 외국인이 샀다·이익 전망이 좋다·
거래량이 붙었다까지가 아는 전부이고 내일 오른다는 보장은 아니다. 그래서
"상승 확률"이 아니라 **"상승 근거가 얼마나 두껍게 쌓였는가"**를 잰다.
틀렸을 때 어느 근거가 틀렸는지 되짚을 수 있어야 다음 판정이 나아진다.

점수는 하나다. 방향성 있는 신호를 부호 있게 더한 값이고, **0 근처는 근거가
없거나 서로 맞선다는 뜻이라 픽에서 빠진다** — 갈림을 따로 판정할 필요가 없다.
추세(+)와 과열(-)이 상쇄되는 것은 의도한 동작이다. 많이 올라온 종목은 둘이
맞물려 중간으로 밀리고, 적당히 오르면서 수급이 붙은 종목이 위로 올라온다.

**자산군에 따라 고르는 목적이 다르다.**

| | 대상 | 무엇을 내나 |
|---|---|---|
| 주식 (국장·미장) | 후보 전체에서 고름 · 3종 | 오를 근거가 두꺼운 것. 살 수 있는 쪽이 한 방향뿐이라 갈리는 방향은 쓸모가 없다 |
| 선물 (코인·매크로) | **고정** — BTC·ETH / 금·WTI원유 | 정해진 것의 방향. 고르지 않는다 |

**선물은 대상을 고정한다.** 매일 다른 알트코인·농산물이 올라오면 어제와 오늘을
이어서 볼 수가 없고 애초에 그것들을 거래하지 않는다. 고정 대상은 **점수가 낮아도
빼지 않고** '팽팽'으로 표시한다 — 방향이 안 잡혔다는 것도 알아야 할 상태다.
WTI원유를 쓰는 이유는 브렌트유에 선물 커브도 COT도 없어 근거가 얇어서다.

주식에서 상승 후보가 3개에 모자라면 남는 칸은 **'피할 것'**으로 채우고 그렇게
표시한다. 기준을 낮춰 채우면 근거 없는 자리가 있는 자리와 같은 무게로 읽힌다.

**예정된 실적·공시는 점수에 넣지 않는다.** 오를 이유가 아니라 결과에 달렸다는
뜻이기 때문이다. 대신 경고로 남겨 뒤집힐 자리를 알려 준다.

**근거가 깨지는 지점은 손절선이 아니다.** "-3% 빠지면 손절" 같은 일률적인 값은
그 종목이 왜 뽑혔는지와 무관해서, 깨져도 무엇이 깨진 것인지 알 수 없고 다음
판정에 쓸 수가 없다. 각 근거는 이미 자기 조건을 갖고 있다 — 20일선 위로 뽑혔으면
20일선이, 5일 연속 순매수로 뽑혔으면 6일째 방향이 그 조건이다. `_breaks`가 그
값을 계산해서 낸다(20일선 위 +24%면 -19.4%가 그 지점이다). **무엇을 할지는
읽는 쪽 몫으로 남긴다.**

**거래량 게이트는 장중 수집일 때만 건다.** 그 가드는 장이 안 끝나 거래량이
덜 찬 경우를 막는 것이지(`dashboard.py`의 `partial`) 거래가 적은 것 자체가
흠이어서가 아니다. 마감된 장에서 0.5배는 결함이 아니라 "조용하다"는
정보다. 이걸 게이트로 쓰면 규제 뉴스가 붙은 KB금융 같은 종목이 통째로
빠진다(실측: 16개 중 6개가 이 이유로 탈락).

**절대 임계값이 상수인 값은 스냅샷 중간값 대비로 잰다.** 모두에게 켜지는
신호는 신호가 아니라 상수다. 실측: 추정PER 갭은 국장 266개 중 92%가 음수라
"-20% 이하면 이익 증가"가 3분의 2에서 켜졌고, 52주 고점 대비는 100%가
음수였다. 외국인·기관 수급도 양수가 58%·69%다(스크리너가 거래대금 상위와
급등을 뽑으니 표본이 매집 쪽으로 기운다). 그대로 세면 국장 방향이 66개 중
59개 '상승'으로 굳는다. `RELATIVE`에 모아 뒀다.

**공시 제목에 뉴스 어휘를 쓰지 마라.** 실측(고유 제목 133개)에서 `score_text`로
점수가 붙은 21개 중 절반이 오탐이었다 — '주식선물ㆍ옵션 가격제한폭 확대요건
도달(상승)'이 +2(파생 절차 안내), '투자위험종목 지정해제'가 -1(해제는 호재인데
부호가 뒤집힘), '주권매매거래정지(무상증자)'가 -3(절차적 정지). 정작
'단일판매ㆍ공급계약 해지'는 0이었다. 뉴스 어휘의 '계약 체결'·'공급 계약'에는
공백이 있는데 공시 제목에는 없기 때문이다. `rules.score_disclosure`가 좁은
공시 전용 규칙으로 판정하고, **가장 센 것 하나만 센다** — 공시 하나는 한
사건이라 더하면 '자기주식취득 신탁계약 체결'이 +4가 된다.

**같은 사실을 두 번 세지 마라.** 미장 상대강도를 당일로 재니 시장 시총가중
등락률이 0 근처라(실측 -0.03%) 종목 등락률과 사실상 같은 값이 되어, 하루 급등
하나가 `volume_flow`·당일 등락·상대강도로 **세 번** 세졌다. 상대강도는 20일
기준(S&P500 대비)으로 재고, 당일 등락은 `volume_ratio`가 없을 때만 센다
(코인이 그렇다 — 거기서는 `volume_flow`가 아예 안 켜진다).

**자산군마다 잴 수 있는 축의 수가 다르다.** 미장은 수급도 펀더멘털도 없어
(야후 `quoteSummary` 401) 거래량이 그 자리를 대신하고(`read.volume_flow`),
**20일 상대강도와 섹터 흐름**을 더한다 — 둘 다 이미 수집돼 있는데 안 쓰고
있었다. 이걸 붙이자 미장 픽의 근거가 중간 3.5개에서 6개로 늘었다.
매크로는 60일선도 거래량도 수급도 없어 시세만으로는 지표당 근거가 두 개뿐이라,
**재고·선물커브·COT 주간 변화**를 함께 쓴다 — 재고는 '사실', 커브는 '기대',
COT는 '포지션'이라 서로 다른 것을 잰다. 이 셋을 붙이자 근거 2개 이하인
매크로 픽이 76%에서 26%로 줄었다.

**기준은 `MIN_SCORE`로 고르고 모자라는 자리만 `FALLBACK_SCORE`로 채운다.**
3점으로 못박으면 매크로가 14회 중 3회만 3종을 채우고, 2점으로 낮추면 픽의
4분의 1이 근거 두 개짜리가 된다. 채운 자리는 프롬프트에 표시되어 글이 같은
무게로 쓰지 않는다.

**코인 임계값을 `read.py`에서 그대로 가져오지 마라.** 그쪽 기준(펀딩비 ±5bp,
롱숏비 0.4)은 바이낸스 스케일이라 OKX 실측(펀딩비 -2.6~+3.0bp, 롱숏비 5%가
0.69)에서는 **한 번도 걸리지 않았다.** 죽은 넷이 전부 상승을 가리키는 역방향
신호라 코인·매크로에서 반등 근거를 잡을 방법이 아예 없었다. 중간값 대비로
바꾸니 96번 켜졌다.

### 시각은 mtime으로 재지 마라

git이 파일을 받아오면 mtime이 그때로 찍힌다. 클라우드가 대시보드를
다시 구울 때마다 일주일 전 인사이트가 "방금 작성"으로 보였던 원인이다.

- 수집 시각 → 파일명에 박힌 값 (`core/store.py`)
- 인사이트 작성 시각 → 마지막 커밋 시각 (`dashboard.py:_written_at`)
- `actions/checkout`은 `fetch-depth: 0` 이라야 `git log`가 이력을 본다

---

## 환경

- **PowerShell 5.1** — `&&`는 파서 오류다. `;` 또는 `if ($?) { }`.
  한글이 든 커밋 메시지는 `-m` 대신 파일로 넘긴다(`git commit -F`).
  `Set-Content -Encoding UTF8`은 BOM을 붙인다 — 파이썬 파일에 쓰면
  `SyntaxError: invalid non-printable character U+FEFF`가 난다.
- **의존성은 `requirements.txt` 둘뿐** — `requests`, `feedparser`.
  워크플로도 이 파일을 쓴다. 새로 추가하면 양쪽이 어긋나지 않게 여기 넣는다.
- **API 키 셋** — `DART_API_KEY`(40자) · `ECOS_API_KEY`(20자) ·
  `EIA_API_KEY`(40자). PC는 `.env`, 클라우드는 GitHub Secrets에 있어야
  한다. 없으면 해당 소스만 조용히 빠진다.
- `.env`의 키는 채팅에 붙여넣지 않는다. GitHub Secrets는 Name에 이름,
  Secret에 **값만** (이름을 값에 같이 넣어 세 키가 전부 거부된 적 있다).
  `probe.py`가 키의 길이와 포함 문자를 재서 알려 준다 — 값은 안 보여 준다.
- `ANTHROPIC_API_KEY`는 지금 비어 있다. 인사이트는 Claude가 직접 쓰고
  `insight.py`(API 호출)는 쓰지 않는다 — **요금이 붙는다.**

---

## 구조

```
collect_{kr,us,macro,coin}.py   수집 → data/{market}_시각.json + prompt_*.txt
core/session.py                 개장까지 남은 시간 → 대비형 프롬프트
core/read.py                    숫자 → 사람 말 (해석 규칙을 한곳에)
core/pick.py                    오늘의 픽 3종 — 관전 점수와 방향 판정
core/store.py                   data/ 와 cloud/ 중 최신 고르기
core/feed.py                    매크로 데스크용 숫자 피드
dashboard.py                    → data/latest.html · docs/index.html · feed.json
insights/insight_{market}.md    인사이트 글 (커밋됨 — data/ 에 두면 지워진다)
```

`data/`는 `.gitignore` 대상이다. 클라우드 러너에는 없으므로 **커밋해야
하는 산출물은 `cloud/`나 `insights/`에 둔다.**

## 흐름

| | 누가 | 언제 |
|---|---|---|
| 수집 | GitHub Actions | 평일 07:20~08:20 · 20:50~22:50 (여러 번) |
| 인사이트 | Claude 루틴(PC) 또는 `/인사이트` | 새 데이터가 있을 때만 |
| 대시보드 재빌드 | GitHub Actions | `insights/` 가 바뀌면 |

인사이트는 `.claude/skills/인사이트/`의 스킬이 쓴다. PC 루틴은 아침
07:22·08:22·09:22, 저녁 21:12·22:12·23:12에 돌면서 `pending.py`가
가리키는 것만 쓴다 — 대부분은 할 일이 없어 몇 초에 끝난다.
루틴은 **Claude 앱이 켜져 있어야** 돈다. 수집은 PC와 무관하다.

GitHub 예약은 한 시간까지 밀린다(실측: 08:00 예약이 08:58 실행). 그래서
개장 전 구간에 여러 번 걸고 `--auto`가 중복을 거른다. 루틴도 시각이
아니라 `tools/pending.py`가 가리키는 것만 쓴다.

---

## 글쓰기

인사이트는 **회고가 아니라 대비**다. 지나간 장을 복기하지 말고 다가올
장에서 무엇을 볼 것인지 쓴다. 형식은 프롬프트 파일의 `## 요청` 섹션에
있고, 그게 정본이다 — 다른 곳에 베껴 적으면 낡는다.

전문용어를 그대로 쓰지 않는다. 플래트닝 → 장단기 금리차 축소,
백워데이션 → 지금 물건이 나중보다 비쌈, 타이트 → 부족. 커밋 전에
`python tools/lint_insight.py`를 통과시킨다.

**방향은 '오늘의 픽'에만 붙는다.** 그 3종은 `core/pick.py`가 규칙으로
계산한 것이고, 글은 순위와 방향을 바꾸지 않는다. 픽 밖의 항목은 여전히
"무엇을 보라"까지가 범위다.

사라·팔라·비중·진입가·목표가·손절가는 쓰지 않는다. '상승 우세'는 데이터가
가리키는 쪽이고 '지금 사십시오'는 매매 지시다. 방향에는 **틀렸다고 볼
조건**을 반드시 함께 적는다 — 조건이 없으면 나중에 맞았는지 틀렸는지
아무도 못 재고, 그러면 다음 판정이 나아지지 않는다.

---

## 자주 걸리는 것

**`docs/index.html` 충돌** — 생성물이다. 병합하지 말고
`python run.py --skip-collect --no-insight --no-deploy`로 다시 굽고
`git add`한다. `.gitattributes`가 자동 병합을 막아 둔 이유다.

**푸시 거부** — 클라우드가 먼저 커밋했을 수 있다. `git pull --rebase`
후 재시도. 리베이스 중 `docs/` 충돌이면 위와 같이 처리한다.

**워크플로 실행이 `cancelled`** — 실패가 아니다. 예약이 여러 개라
같은 `concurrency` 그룹에 몰리면 대기 중이던 것이 취소된다. 앞뒤
실행이 이미 받았으므로 데이터 손실은 없다.

**미장 거래량이 전부 낮게 나옴** — 장중에 수집된 것이다. 완결된 장이면
거래량 배율의 중앙값이 1.0 근처다. 0.7 미만이면 판정을 접는다
(`dashboard.py`의 `partial`). 최대값으로 재면 안 된다 — 급등주 하나가
30분 만에 평균을 넘겨(실측: 개장 32분에 PLTR 1.17배) 가드가 뚫린다.

---

## 알려진 한계

고칠 수 있는데 안 한 것이 아니라, 소스가 없거나 막혀서 못 하는 것들이다.
새로 발견한 척 다시 파고들지 않도록 적어 둔다.

- **한국 공휴일을 모른다.** `core/session.py`가 주말만 거른다. 광복절에도
  "개장 1시간 전"이라고 쓴다.
- **미장 펀더멘털이 없다.** 야후 `quoteSummary`가 401이라 PER·PBR을 못
  받는다. 국장은 네이버에서 받는데 미장만 비어 있다.
  대신 **나스닥 API에 아직 안 붙인 두 소스가 있다**(2026-08-08 PC 실측, 키 불필요):
  `api.nasdaq.com/api/analyst/{sym}/targetprice` — 목표주가·매수/보유/매도 수·
  등급 변화 이력, `api.nasdaq.com/api/company/{sym}/earnings-surprise` — 최근
  4분기 EPS 대 컨센서스와 서프라이즈 %. 시험한 7종목 중 6종목에서 나왔고
  스핀오프(HONA)만 비었다. **러너에서는 아직 안 재 봤다** — 붙이려면
  `tools/probe.py`에 먼저 넣어야 한다.
- **FRED가 막힌다.** PC·러너 양쪽에서 타임아웃이라 기대인플레(BEI)와
  실질금리를 못 받는다. 매크로 데스크의 `realYield`·`inflation`이 이것
  때문에 비어 나간다.
- **OKX는 바이낸스보다 종목이 적다**(421 vs 680). 작은 코인은 클라우드
  수집에서 파생 데이터가 아예 없다.
- **원유 재고 서프라이즈는 화·수요일에만** 들어온다(API·EIA 발표일).
  다른 요일에 `inventory`가 빠지는 것은 정상이다.

---

## 커밋 메시지

무엇을 바꿨는지가 아니라 **왜 바꿨는지**를 쓴다. 코드는 무엇을 하는지
이미 말하고 있다. 실측값이 있으면 넣는다 — "GitHub 예약이 밀린다"보다
"08:00 예약이 08:58에 돌았다"가 다음 사람에게 쓸모 있다.
